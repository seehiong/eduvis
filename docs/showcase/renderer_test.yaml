# EduVis-Core: Comprehensive renderer test
# One slide per registered element type — verifies all 13 renderers.
# Note: summary_list is an alias for text_list and is omitted.
# Note: math_box does NOT exist; registered math types are listed below.

lesson:
  syllabus: SEC-math-2027
  topic: N1.6
  title: Renderer Test

content:

  # 1 ── number_line ──────────────────────────────────────────────────────────
  - id: test_number_line
    type: number_line
    placement:
      lesson_phase: explore
      visual_weight: primary
      memory_role: example
    range: [-5, 5]
    highlight:
      - value: -3
        label: "Start"
        color: blue
      - value: 2
        label: "End"
        color: red

  # 2 ── text_list ────────────────────────────────────────────────────────────
  - id: test_text_list
    type: text_list
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    items:
      - "Numbers to the left on the number line are smaller"
      - "Numbers to the right are larger"
      - "-7 is smaller than -2 because -7 is further left"
      - "no-bullet: The direction is what matters, not the size of the digit"

  # 3 ── fact_boxes ───────────────────────────────────────────────────────────
  - id: test_fact_boxes
    type: fact_boxes
    placement:
      lesson_phase: hook
      visual_weight: primary
      memory_role: anchor
    items:
      - text: "Singapore: 32°C"
        color: red
      - text: "Antarctica: -12°C"
        color: blue

  # 4 ── example_panel ────────────────────────────────────────────────────────
  - id: test_example_panel
    type: example_panel
    placement:
      lesson_phase: guided_practice
      visual_weight: primary
      memory_role: example
    items:
      - heading: "Which is larger: -7 or -2?"
        body: "Step 1: Plot both on a number line.\nStep 2: -2 is to the right of -7.\nAnswer: -2 > -7"
      - heading: "Which is larger: -1 or -4?"
        body: "Step 1: Plot both on a number line.\nStep 2: -1 is to the right of -4.\nAnswer: -1 > -4"

  # 5 ── callout_box ──────────────────────────────────────────────────────────
  - id: test_callout_box
    type: callout_box
    placement:
      lesson_phase: explain
      visual_weight: supporting
      memory_role: anchor
    title: "The Rule"
    border_color: cyan
    lines:
      - "A number further left on the number line is always smaller."
      - "Example: -7 < -2 because -7 is further left."
      - "Example: -1 > -5 because -1 is further right."

  # 6 ── multiple_choice ──────────────────────────────────────────────────────
  - id: test_multiple_choice
    type: multiple_choice
    placement:
      lesson_phase: independent_practice
      difficulty: starter
      visual_weight: primary
      memory_role: practice
    question: "Which is larger: -1 or -5?"
    options:
      A: "-1"
      B: "-5"
      C: "They are equal"
      D: "Cannot tell"
    answer: "A"

  # 7 ── hint_list ────────────────────────────────────────────────────────────
  - id: test_hint_list
    type: hint_list
    placement:
      lesson_phase: guided_practice
      visual_weight: primary
      memory_role: example
    items:
      - "Plot both numbers on a number line"
      - "Identify which is further to the right"
      - "The number further right is always larger"
    final: "Remember: further right = larger value"

  # 8 ── coordinate_plane ─────────────────────────────────────────────────────
  - id: test_coordinate_plane
    type: coordinate_plane
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    x_range: [-4, 4]
    y_range: [-4, 4]
    grid_step: 1
    plots:
      - type: line
        equation: "y = x"
        color: cyan
      - type: point
        coord: [2, 2]
        label: "(2, 2)"
        color: green
      - type: point
        coord: [-3, -3]
        label: "(-3, -3)"
        color: red

  # 9 ── fraction_model ───────────────────────────────────────────────────────
  - id: test_fraction_model
    type: fraction_model
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    shape: circle
    total_parts: 4
    shaded_parts: 3
    color: green
    label: "3 out of 4 parts shaded"

  # 10 ── bar_model ───────────────────────────────────────────────────────────
  - id: test_bar_model
    type: bar_model
    placement:
      lesson_phase: explore
      visual_weight: primary
      memory_role: example
    style: comparison
    bars:
      - label: "Singapore"
        value: 32
        color: red
      - label: "Antarctica"
        value: 12
        color: blue
    difference:
      label: "20 degrees apart"
      color: yellow

  # 11 ── geometry_shape ──────────────────────────────────────────────────────
  - id: test_geometry_shape
    type: geometry_shape
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    vertices: [[100, 300], [300, 100], [500, 300]]
    labels: ["A", "B", "C"]
    side_labels:
      - edge: "AB"
        label: "5 cm"
      - edge: "BC"
        label: "5 cm"
      - edge: "CA"
        label: "8 cm"
    angles:
      - vertex: "A"
        label: "∠CAB = 53°"
      - vertex: "B"
        label: "87°"
      - vertex: "C"
        label: "∠BCA = ?"

  # 11b ── geometry_shape_external ────────────────────────────────────────────
  - id: test_geometry_shape_external
    type: geometry_shape
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    vertices: [[100, 300], [300, 100], [500, 300], [600, 300]]
    labels:
      - "A"
      - "B"
      - text: "C"
        offset: [0, 18]
      - "D"
    edges: ["AB", "BC", "CA", "CD"]
    side_labels:
      - edge: "AB"
        label: "5 cm"
      - edge: "BC"
        label: "5 cm"
      - edge: "CA"
        label: "8 cm"
    angles:
      - vertex: "C"
        sides: ["B", "D"]
        label: "140°"

  # 12 ── factor_array ────────────────────────────────────────────────────────
  - id: test_factor_array
    type: factor_array
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    number: 12
    color: green
    verdict: true

  # 13 ── math_grid (arithmetic) ─────────────────────────────────────────────
  - id: test_math_grid
    type: math_grid
    placement:
      lesson_phase: guided_practice
      visual_weight: primary
      memory_role: example
    headers: ["H", "T", "U"]
    rows:
      - ["", "3", "7"]
      - ["+", "1", "5"]
      - "line"
      - ["", "5", "2"]
    row_colors: ["cyan", "cyan", "", "yellow"]

  # 14 ── math_grid (ratio) ────────────────────────────────────────────────────
  - id: test_math_grid_ratio
    type: math_grid
    placement:
      lesson_phase: guided_practice
      visual_weight: primary
      memory_role: example
    mode: ratio
    show_grid: false
    headers: ["Concentrate", "Water"]
    rows:
      - ["1", "3"]
      - ["2", "6"]
      - ["3", "9"]
    row_colors: ["body", "cyan", "cyan"]

  # 15 ── solid_shape (cube) ───────────────────────────────────────────────────
  - id: test_solid_cube
    type: solid_shape
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    shape: cube
    dimensions: [4]
    color: blue
    label: "4 cm"
    show_dimensions: true

  # 16 ── solid_shape (rectangular_prism) ──────────────────────────────────────
  - id: test_solid_rectangular_prism
    type: solid_shape
    placement:
      lesson_phase: explain
      visual_weight: primary
      memory_role: anchor
    shape: rectangular_prism
    dimensions: [5, 3, 4]
    color: green
    label: "Rectangular Prism (w×h×d)"
    show_dimensions: true

  # 17 ── solid_shape (pyramid) ────────────────────────────────────────────────
  - id: test_solid_pyramid
    type: solid_shape
    placement:
      lesson_phase: explore
      visual_weight: primary
      memory_role: example
    shape: pyramid
    dimensions: [4, 5, 6]
    color: orange
    label: "Rectangular pyramid (w×d×h)"
    show_dimensions: true

  # 18 ── solid_shape (cone) ──────────────────────────────────────────────────
  - id: test_solid_cone
    type: solid_shape
    placement:
      lesson_phase: explore
      visual_weight: primary
      memory_role: example
    shape: cone
    dimensions: [4, 6, 4]
    color: red
    label: "Cone with dimensions"
    show_dimensions: true

  # 19 ── solid_shape (cylinder) ──────────────────────────────────────────────
  - id: test_solid_cylinder
    type: solid_shape
    placement:
      lesson_phase: explore
      visual_weight: primary
      memory_role: example
    shape: cylinder
    dimensions: [3, 5, 3]
    color: cyan
    label: "Cylinder with dimensions"
    show_dimensions: true
