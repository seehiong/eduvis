# Workspace Rules & Security Constraints

## Sensitive Data & Environment Protection
- Always ensure `.env`, `.env.*`, API keys, private certificates, and sensitive credentials are listed in `.gitignore`.
- Never commit `.env` files, API tokens, or secrets to Git repositories under any circumstances.
